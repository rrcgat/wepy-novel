// Configuration 134.175.122.118

const host = 'http://192.168.1.122:5000/api'

export const login = `${host}/login`
export const index = `${host}/index`
export const book = `${host}/book`
export const author = `${host}/author`
export const star = `${host}/star`

const hosts = {
  login: login,
  index: index,
  book: book,
  author: author,
  star: star
}

export default hosts
